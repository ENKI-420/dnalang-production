# AURA Swarm CLI Configuration

# API endpoint (defaults to localhost:3000 if not set)
AURA_API_URL=https://your-deployment-url.vercel.app

# Optional: IBM Quantum credentials for organism deployment
IBM_QUANTUM_TOKEN=your_ibm_quantum_token_here
